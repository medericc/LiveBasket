import 'package:flutter/material.dart';

class PlayersScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Liste des joueurs classés par stats'),
      ),
    );
  }
}
